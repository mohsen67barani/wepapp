import { ARTICLES } from "@/articles";
import { Card } from "@/components/ui/card";
import { Link } from "wouter";

export default function Blog() {
  return (
    <div className="min-h-screen bg-background text-foreground">
      <header className="sticky top-0 z-50 bg-white/80 backdrop-blur-md border-b border-border">
        <div className="container max-w-6xl mx-auto px-4 py-4 flex items-center justify-between">
          <div className="text-xl font-bold text-accent">Mohsen Baranizadeh</div>
          <nav className="flex gap-8">
            <Link href="/" className="hover:text-accent transition">
              Home
            </Link>
            <Link href="/blog" className="text-accent font-semibold transition">
              Blog
            </Link>
          </nav>
        </div>
      </header>

      <section className="py-16 md:py-24 bg-gradient-to-b from-blue-50 to-white">
        <div className="container max-w-4xl mx-auto px-4">
          <div className="text-center space-y-4 mb-12">
            <h1 className="text-4xl md:text-5xl font-bold text-foreground">
              AI & Tech Insights
            </h1>
            <p className="text-lg text-muted-foreground max-w-2xl mx-auto">
              My latest thoughts on LLM DevOps, Predictive Analytics, and AI Strategy.
            </p>
          </div>

          <div className="space-y-8">
            {ARTICLES.map((article) => (
              <Card key={article.slug} className="p-8 hover:shadow-lg transition border-border bg-white">
                <Link href={`/blog/${article.slug}`}>
                  <a className="block">
                    <h2 className="text-2xl font-bold text-foreground hover:text-accent transition">
                      {article.title}
                    </h2>
                  </a>
                </Link>
                <p className="text-sm text-muted-foreground mt-1 mb-4">
                  Published on {article.date}
                </p>
                <p className="text-muted-foreground mb-4">
                  {article.excerpt}
                </p>
                <Link href={`/blog/${article.slug}`}>
                  <a className="text-accent font-semibold hover:underline">
                    Read Article &rarr;
                  </a>
                </Link>
              </Card>
            ))}
          </div>
        </div>
      </section>

      {/* Footer - Reusing the Home page footer structure for consistency */}
      <footer className="bg-foreground text-white py-12">
        <div className="container max-w-6xl mx-auto px-4">
          <div className="text-center text-white/50 text-sm">
            <p>&copy; 2025 Mohsen Baranizadeh. All rights reserved.</p>
          </div>
        </div>
      </footer>
    </div>
  );
}
