import * as React from "react";

// This is a minimal placeholder for the TooltipProvider component.
// It is not strictly necessary for the build but prevents import errors.

export function TooltipProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
